# Runtime
MetaTrader5 ; platform_system == "Windows"
pandas>=2.0
python-dotenv>=1.0
tzdata ; platform_system == "Windows"   # EST/EDT zone data for the dashboard clock
databento>=0.50

# Dev / test
pytest>=7.0
